<div class="announcement_bar bg-gradient-to-r from-slate-50 to-white border-b border-slate-200 hidden md:block">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center py-3">

            <!-- Benefits List -->
            <div class="flex items-center gap-6 lg:gap-8">
                <!-- Benefit 1 -->
                {%- if section.settings.benefit_1_text != blank -%}
                <div class="flex items-center gap-2 group">
                    <div class="bg-slate-700 rounded-lg p-1.5 group-hover:bg-slate-600 transition-colors">
                        <svg class="w-4 h-4 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
                        </svg>
                    </div>
                    <span class="text-slate-700 font-medium text-sm">{{ section.settings.benefit_1_text }}</span>
                </div>
                {%- endif -%}

                <!-- Benefit 2 -->
                {%- if section.settings.benefit_2_text != blank -%}
                <div class="flex items-center gap-2 group">
                    <div class="bg-slate-700 rounded-lg p-1.5 group-hover:bg-slate-600 transition-colors">
                        <svg class="w-4 h-4 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M13 10V3L4 14h7v7l9-11h-7z" />
                        </svg>
                    </div>
                    <span class="text-slate-700 font-medium text-sm">{{ section.settings.benefit_2_text }}</span>
                </div>
                {%- endif -%}

                <!-- Benefit 3 -->
                {%- if section.settings.benefit_3_text != blank -%}
                <div class="flex items-center gap-2 group">
                    <div class="bg-slate-700 rounded-lg p-1.5 group-hover:bg-slate-600 transition-colors">
                        <svg class="w-4 h-4 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z" />
                        </svg>
                    </div>
                    <span class="text-slate-700 font-medium text-sm">{{ section.settings.benefit_3_text }}</span>
                </div>
                {%- endif -%}

                <!-- Benefit 4 -->
                {%- if section.settings.benefit_4_text != blank -%}
                <div class="flex items-center gap-2 group">
                    <div class="bg-slate-700 rounded-lg p-1.5 group-hover:bg-slate-600 transition-colors">
                        <svg class="w-4 h-4 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M12 6V4m0 2a2 2 0 100 4m0-4a2 2 0 110 4m-6 8a2 2 0 100-4m0 4a2 2 0 110-4m0 4v2m0-6V4m6 6v10m6-2a2 2 0 100-4m0 4a2 2 0 110-4m0 4v2m0-6V4" />
                        </svg>
                    </div>
                    <span class="text-slate-700 font-medium text-sm">{{ section.settings.benefit_4_text }}</span>
                </div>
                {%- endif -%}
            </div>

            <!-- Contact Button -->
            {%- if section.settings.show_contact_button -%}
            <a href="{{ section.settings.contact_link | default: '/pages/contact' }}" class="group bg-slate-700 hover:bg-slate-800 text-white font-semibold py-2 px-6 rounded-lg flex items-center gap-2 transition-all duration-300 ease-in-out shadow-md hover:shadow-lg hover:-translate-y-0.5">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
                </svg>
                <span class="text-sm">{{ section.settings.contact_text | default: 'Contact' }}</span>
                <svg class="w-4 h-4 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2.5">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M17 8l4 4m0 0l-4 4m4-4H3" />
                </svg>
            </a>
            {%- endif -%}
        </div>
    </div>
</div>

{% schema %}
{
  "name": "Announcement Bar",
  "tag": "div",
  "class": "announcement-bar-section",
  "settings": [
    {
      "type": "header",
      "content": "Benefits"
    },
    {
      "type": "text",
      "id": "benefit_1_text",
      "label": "Benefit 1 Text",
      "default": "Gratis proefrit"
    },
    {
      "type": "text",
      "id": "benefit_2_text",
      "label": "Benefit 2 Text",
      "default": "Korte levertijd"
    },
    {
      "type": "text",
      "id": "benefit_3_text",
      "label": "Benefit 3 Text",
      "default": "10 jaar garantie"
    },
    {
      "type": "text",
      "id": "benefit_4_text",
      "label": "Benefit 4 Text",
      "default": "Fiets samenstellen"
    },
    {
      "type": "header",
      "content": "Contact Button"
    },
    {
      "type": "checkbox",
      "id": "show_contact_button",
      "label": "Show Contact Button",
      "default": true
    },
    {
      "type": "text",
      "id": "contact_text",
      "label": "Contact Button Text",
      "default": "Contact"
    },
    {
      "type": "url",
      "id": "contact_link",
      "label": "Contact Button Link"
    }
  ],
  "presets": [
    {
      "name": "Announcement Bar"
    }
  ]
}
{% endschema %}
